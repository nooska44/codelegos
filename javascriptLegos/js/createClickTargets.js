function checkForRose(click){
	if(click.target.classList.contains('rose')){ 
    // set click target as multiple smurfs listening 
    // why dont need a dot for class - already established looking for a class with classlist
