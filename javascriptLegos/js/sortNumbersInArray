function arrayOfNums(numbers) {
  let numLowToHigh = numbers.sort((a, b) => a - b)  // sort smallest number to largest 
  let numHighToLow = numbers.sort((a, b) => b - a) // sort largest to smallest 
}

