//You can call a function in a variable declaration  

function multiplyThreeNums(num1, num2, num3){
    return(num1 * num2 * num3)
}

let returnedVal = multiplyThreeNums(2,3,2)
console.log(returnedVal)
